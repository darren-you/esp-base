- 移除 C3 Base 启动期完整配置副本并复用 MQTT 创建与事件缓冲，复测五组件 QEMU 容量
- 消除 C3 Base 配置路径的三份静态冗余缓冲，复测五组件 QEMU 容量并记录剩余并发边界
- 将 Base FRP 精确依赖更新到活动双流重启回归版本，并完成主机与 C3 复验
- 将 Base OTA 精确依赖升级到 TLS 会话票据期限修正版本，并完成主机与 C3 复验
- 将 ESP Base SDK 锁至公开 OTA 修复 fork，同步 MQTT 精确依赖并完成 C3 复验
- 拒绝 SDK 源码与子模块漂移，并同步活动方案中的 MQTT 精确版本
- 将 Base OTA 通用下载与槽机制硬切到精确公开 esp-ota，保留产品约束、持久收据和本地自检
- 在 MQTT QoS 1 outbox 过期时撤销 Base ready 并重建会话，补宿主故障回归
- 将 Base 的 esp-ota 精确依赖更新到槽预检期限修正版本
- 将 Base 的 esp-ota 精确依赖更新到镜像校验与完整签名摘要接口版本
- 增加双应用槽可启动固件身份的只读观察与保守拒绝，补充宿主回归
- 增加 Base 与 Container 可选固件集合适配及共用串行 owner，补充主机和固定 SDK 编译验证
- 增加 FRP 回环只读状态端点，复用独立 HMAC 和设备结果合同并验证请求边界
- 将 C3 分支的 MQTT、FRP 与可选 Container 精确锁对齐公开分支，并完成主机和固定 SDK 组合编译
